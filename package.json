{
  "name": "picgo-plugin-confluence",
  "version": "1.1.8",
  "description": "picgo uploader for confluence",
  "main": "src/index.js",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/ridup/picgo-plugin-confluence",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "patch": "npm version patch && npm publish && git push origin main && git push origin --tags",
    "minor": "npm version minor && npm publish && git push origin main && git push origin --tags",
    "major": "npm version major && npm publish && git push origin main && git push origin --tags"
  },
  "keywords": [
    "picgo",
    "picgo-gui-plugin",
    "picgo-plugin"
  ],
  "author": "Ridup",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.0.1",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-node": "^6.0.1",
    "eslint-plugin-promise": "^3.8.0",
    "eslint-plugin-standard": "^3.1.0",
    "@varnxy/logger": "^1.1.2"
  }
}
